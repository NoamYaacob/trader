// Public API for the strategy feature.
// Other features import from here, not from internal subdirectories.
export type { StrategyRecord, StrategyIntakeData, IntakeStepNumber } from "./types";
export { INTAKE_STEPS, TOTAL_STEPS } from "./types";
